/* ===========================================
   Adamus - Ratio Table Question Type
   Fill in ratio/proportion tables
   =========================================== */

// This is a stub - will be migrated from quiz.js

export function render(container, q) {
  console.warn("ratio-table.render() stub called");
}

export function check(q) {
  console.warn("ratio-table.check() stub called");
  return { correct: false, score: 0, maxScore: 1 };
}
