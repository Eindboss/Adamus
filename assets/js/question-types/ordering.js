/* ===========================================
   Adamus - Ordering Question Type
   Drag-and-drop or number input ordering
   =========================================== */

// This is a stub - will be migrated from quiz.js

export function render(container, q) {
  console.warn("ordering.render() stub called");
}

export function check(q) {
  console.warn("ordering.check() stub called");
  return { correct: false, score: 0, maxScore: 1 };
}
