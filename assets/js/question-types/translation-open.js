/* ===========================================
   Adamus - Translation Open Question Type
   Free-form text translation
   =========================================== */

// This is a stub - will be migrated from quiz.js

export function render(container, q) {
  console.warn("translation-open.render() stub called");
}

export function check(q) {
  console.warn("translation-open.check() stub called");
  return { correct: false, score: 0, maxScore: 1 };
}
