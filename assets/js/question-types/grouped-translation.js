/* ===========================================
   Adamus - Grouped Translation Question Type
   Translate underlined words in context
   =========================================== */

// This is a stub - will be migrated from quiz.js

export function render(container, q) {
  console.warn("grouped-translation.render() stub called");
}

export function check(q) {
  console.warn("grouped-translation.check() stub called");
  return { correct: false, score: 0, maxScore: 1 };
}
