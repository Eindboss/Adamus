/* ===========================================
   Adamus - Grouped Select Question Type
   Multiple dropdowns per sentence
   =========================================== */

// This is a stub - will be migrated from quiz.js

export function render(container, q) {
  console.warn("grouped-select.render() stub called");
}

export function check(q) {
  console.warn("grouped-select.check() stub called");
  return { correct: false, score: 0, maxScore: 1 };
}
